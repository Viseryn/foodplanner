{# templates/component/_button.html.twig #}

<a href="{{ path|default('#') }}" class="{% if inverse is defined and inverse %}{{ btn_submit_inverse }}{% else %}{{ btn_submit }}{% endif %}">
    {{ include('component/_button_content.html.twig', {
        icon: icon|default(''),
        label: label|default(''),
    }) }}
</a>