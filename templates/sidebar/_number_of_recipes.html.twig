{# templates/sidebar/_number_of_recipes.html.twig #}

{% if numberOfRecipes > 0 %}
    <span class="inline-flex justify-center items-center p-3 ml-3 w-3 h-3 text-sm font-medium text-blue-600 bg-blue-200 rounded-full">
        {{ numberOfRecipes }}
    </span>
{% endif %}