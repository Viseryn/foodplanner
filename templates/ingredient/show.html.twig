{% extends 'base.html.twig' %}

{% block title %}Ingredient{% endblock %}

{% block main %}
    <h1>Ingredient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ingredient.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ingredient.name }}</td>
            </tr>
            <tr>
                <th>QuantityValue</th>
                <td>{{ ingredient.quantityValue }}</td>
            </tr>
            <tr>
                <th>QuantityUnit</th>
                <td>{{ ingredient.quantityUnit }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ingredient_index') }}">back to list</a>

    <a href="{{ path('app_ingredient_edit', {'id': ingredient.id}) }}">edit</a>

    {{ include('ingredient/_delete_form.html.twig') }}
{% endblock %}
