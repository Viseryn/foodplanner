{# templates/recipe/index.html.twig #}

{% extends 'base.html.twig' %}
{% set heading = 'Recipes' %}

{% block title %}{{ heading }}{% endblock %}

{% block sidebar_action %}
    {{ include('sidebar/_sidebar_action.html.twig', {
        path: path('app_recipe_new'),
        icon: 'add',
    }) }}
{% endblock %}

{% block load_sidebar %}loadSidebar('recipes');{% endblock %}

{% block main %}
    {{ include('component/_heading.html.twig', {heading: heading}) }}

    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 xl:grid-cols-4 gap-4 mb-10" style="max-width: 1200px">
        {% for recipe in recipes %}
            <div class="h-40 w-full shadow-md rounded-2xl border border-gray-200 transition duration-300 hover:scale-95 hover:shadow-lg hover:bg-gray-50">
                {% if recipe.image is not null %}
                    <a href="{{ path('app_recipe_show', {'id': recipe.id}) }}" class="relative">
                        <div class="absolute bottom-4 px-6 text-white font-semibold text-xl z-10">{{ recipe.title }}</div>
                        <img class="rounded-2xl h-40 w-full object-cover brightness-75" src="{{ asset(recipe.image) }}" alt="{{ recipe }}" />
                    </a>
                {% else %}
                    <a href="{{ path('app_recipe_show', {'id': recipe.id}) }}" class="h-full w-full p-6 font-semibold text-xl z-10 flex items-end">
                        <div>{{ recipe.title }}</div>
                    </a>
                {% endif %}
            </div>
        {% endfor %}
    </div>
{% endblock %}
